# 돌돔의 공간 프로젝트 지침

## 프로젝트 정보

- 프로젝트명: `dolldom-arcade`
- 사이트명: 돌돔의 공간
- 사이트 유형: 반응형 HTML5 미니게임 사이트
- 배포 방식: Codex Sites
- 공개 주소: `https://dolldom-arcade.gla9940.chatgpt.site/`
- Flash와 SWF를 사용하지 않고 최신 데스크톱 및 모바일 브라우저를 지원한다.
- 프로젝트 전체에서 영문 표기는 `doldom`이 아니라 `dolldom`을 사용한다.

## 디자인 원칙

- 네온 사이버펑크 분위기를 유지한다.
- 주요 색상은 검정, 보라, 형광 연두다.
- 제목에는 픽셀 느낌을 사용하고 본문은 읽기 쉬운 글꼴을 사용한다.
- 게임 화면이 페이지에서 가장 눈에 띄도록 구성한다.
- 데스크톱의 큰 화면에서도 게임 영역이 지나치게 작아지지 않게 한다.
- 모바일에서는 버튼과 터치 조작 영역을 충분히 크게 제공한다.
- 게임 플레이 영역은 특별한 요청이 없는 한 수평으로 표시한다.
- 버튼에는 짧고 경쾌한 상호작용 애니메이션을 적용한다.
- 과도한 장식과 복잡한 관리자 기능은 추가하지 않는다.

## 게임 구성

다음 HTML5 미니게임 3개를 유지한다.

1. 장애물을 피하면서 점수를 얻는 러닝 게임
2. 제한 시간 안에 같은 그림을 맞추는 카드 게임
3. 떨어지는 블록을 클릭하는 순발력 게임

각 게임은 다음 기능을 지원해야 한다.

- 키보드 조작과 모바일 터치 조작
- 게임 시작, 일시정지, 다시 시작, 음소거
- 현재 기기의 `localStorage`를 이용한 최고 점수 저장
- 게임 종료 후 점수, 최고 기록, 다시 플레이 버튼 표시
- 같은 기능의 시작 버튼을 화면에 중복해서 표시하지 않기

## 작업 원칙

- 파일을 수정하기 전에 현재 구현과 기존 변경사항을 먼저 확인한다.
- 요청 범위 밖의 기존 디자인과 게임 기능을 보존한다.
- 데스크톱과 모바일 반응형 레이아웃을 함께 확인한다.
- 변경 후 가능한 범위에서 빌드와 주요 게임 흐름을 검증한다.
- 사용자 변경사항과 관계없는 파일은 수정하지 않는다.

## Sites 배포 주의사항

- 기존 `.openai/hosting.json`을 삭제하거나 임의로 새로 만들지 않는다.
- `.openai/hosting.json`에 기록된 기존 Sites 프로젝트 ID를 그대로 사용한다.
- 동일한 로컬 사이트에 새 Sites 프로젝트를 중복 생성하지 않는다.
- 배포 전에 현재 소스 상태를 확인하고 로컬 빌드를 검증한다.
- 배포가 필요한 변경은 기존 Sites 프로젝트의 새 버전으로 저장하고 배포한다.
- 사이트 접근 권한, 공개 상태, 사용자 도메인은 사용자가 요청한 경우에만 변경한다.
- 사이트 제목 변경과 공개 URL 변경을 같은 작업으로 간주하지 않는다.

## 향후 작업

- GitHub 저장소 생성 및 소스 업로드
- VS Code와 GitHub 연결
- 필요 시 사용자 소유 도메인 연결
