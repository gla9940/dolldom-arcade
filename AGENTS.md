# 돌돔의 공간 프로젝트 지침

## 1. 프로젝트 정보

- 프로젝트명: `dolldom-arcade`
- 사이트명: `돌돔의 공간`
- 사이트 유형: 반응형 HTML5 미니게임 아케이드
- 기본 기술 스택:
  - Vite
  - Vanilla JavaScript
  - HTML5
  - CSS
  - Canvas API
  - ES Modules (`import` / `export`)

- 기본 배포 방식: GitHub Pages
- 기본 공개 주소: `https://gla9940.github.io/dolldom-arcade/`
- GitHub 저장소: `https://github.com/gla9940/dolldom-arcade`
- 백업 배포 방식: Codex Sites
- 백업 공개 주소: `https://dolldom-arcade.gla9940.chatgpt.site/`

Flash와 SWF를 사용하지 않는다.

최신 데스크톱 및 모바일 브라우저를 지원한다.

프로젝트 전체에서 영문 표기는 `doldom`이 아니라 반드시 `dolldom`을 사용한다.

---

# 2. 프로젝트 목표

이 프로젝트는 가볍고 빠르게 실행되는 브라우저 기반 미니게임 사이트다.

다음 목표를 우선한다.

1. 기존 게임과 디자인 유지
2. 정확한 게임 동작
3. 빠른 로딩
4. 단순하고 유지보수하기 쉬운 구조
5. 새로운 미니게임을 쉽게 추가할 수 있는 구조
6. 데스크톱과 모바일 지원
7. 키보드와 터치 조작 지원
8. 적은 외부 의존성
9. GitHub Pages에서 안정적인 배포
10. 사람과 AI 에이전트 모두 이해하기 쉬운 코드

필요 이상으로 프로젝트를 복잡하게 만들지 않는다.

---

# 3. 기술 스택

특별한 기술적 이유가 없거나 사용자가 별도로 요청하지 않는 한 다음 기술을 사용한다.

- Vite
- Vanilla JavaScript
- HTML5
- CSS
- Canvas API
- ES Modules

다음 프레임워크는 명시적인 요청 없이 도입하지 않는다.

- React
- Vue
- Next.js
- Angular
- 기타 대형 프론트엔드 프레임워크

간단한 기능을 위해 무거운 외부 라이브러리를 추가하지 않는다.

브라우저 기본 API로 충분히 구현할 수 있다면 기본 API를 우선한다.

---

# 4. 기본 아키텍처

일반 사이트 UI와 실제 게임 플레이 영역을 구분한다.

## HTML/CSS가 담당하는 영역

다음 요소는 기본적으로 HTML/CSS로 구현한다.

- 사이트 헤더
- 게임 선택 화면
- 게임 제목
- 설명
- 시작 버튼
- 다시 시작 버튼
- 일시정지 버튼
- 음소거 버튼
- 점수 표시
- 최고 기록
- 게임 오버 화면
- 설정 UI
- 모달
- 모바일 터치 버튼

## Canvas가 담당하는 영역

다음과 같이 실시간으로 계속 움직이는 게임 요소에는 Canvas를 우선 고려한다.

- 플레이어
- 캐릭터 이동
- 장애물
- 적
- 총알
- 떨어지는 오브젝트
- 충돌 판정
- 파티클
- 배경 스크롤
- 러너 게임
- 슈팅 게임
- 피하기 게임
- 실시간 애니메이션

Canvas가 필요 없는 게임을 억지로 Canvas 기반으로 변경하지 않는다.

예를 들어 다음과 같은 게임은 HTML/CSS 기반으로 구현할 수 있다.

- 카드 뒤집기
- 퀴즈
- 타자 게임
- 단순 클릭 게임
- 숫자 맞히기

---

# 5. 권장 프로젝트 구조

현재 프로젝트 규모와 기존 구현을 우선하되 가능하면 다음 구조를 사용한다.

```text
/
├─ index.html
├─ package.json
├─ vite.config.js
│
├─ public/
│  └─ assets/
│
└─ src/
   ├─ main.js
   │
   ├─ styles/
   │  ├─ global.css
   │  ├─ layout.css
   │  └─ game.css
   │
   ├─ core/
   │  ├─ input.js
   │  ├─ gameLoop.js
   │  ├─ storage.js
   │  └─ sound.js
   │
   └─ games/
      ├─ runner/
      │  └─ game.js
      │
      ├─ memory/
      │  └─ game.js
      │
      └─ reaction/
         └─ game.js
```

이 구조를 맞추기 위해 필요 없는 빈 파일이나 추상화 계층을 억지로 만들지 않는다.

프로젝트 규모가 작은 경우 더 단순한 구조를 허용한다.

---

# 6. 미니게임 독립성

각 게임은 가능한 한 독립적인 모듈로 관리한다.

하나의 게임이 다른 게임의 내부 구현에 직접 의존하지 않도록 한다.

공통 기능은 `src/core/` 등의 공통 모듈에서 관리한다.

예:

- 입력
- 게임 루프
- 사운드
- localStorage
- 공통 UI
- 공통 유틸리티

게임별 상태는 해당 게임 내부에서 관리한다.

게임별 상태를 전역 변수로 남발하지 않는다.

새로운 게임을 추가할 때 기존 게임 전체를 복사한 뒤 이름만 바꾸는 방식은 피한다.

---

# 7. 게임 생명주기

게임은 필요에 따라 다음과 같은 생명주기를 가질 수 있다.

```text
init()
start()
pause()
resume()
restart()
destroy()
```

모든 함수를 반드시 구현할 필요는 없다.

다만 다음 요소를 생성하는 게임은 반드시 정리 가능한 구조를 가져야 한다.

- `requestAnimationFrame`
- `setTimeout`
- `setInterval`
- 키보드 이벤트
- 터치 이벤트
- 전역 이벤트
- Observer
- Audio
- 동적 DOM

게임을 나가거나 다른 게임으로 전환했는데 이전 게임의 로직이 백그라운드에서 계속 실행되면 안 된다.

---

# 8. 게임 구성

현재 다음 HTML5 미니게임 3개를 유지한다.

1. 장애물을 피하면서 점수를 얻는 러닝 게임
2. 제한 시간 안에 같은 그림을 맞추는 카드 게임
3. 떨어지는 블록을 클릭하는 순발력 게임

기존 게임은 사용자의 별도 요청 없이 삭제하거나 다른 게임으로 교체하지 않는다.

게임 규칙과 기존 플레이 감각도 특별한 이유 없이 변경하지 않는다.

---

# 9. 모든 게임의 공통 기능

각 게임은 가능한 범위에서 다음 기능을 지원한다.

- 게임 시작
- 일시정지
- 다시 시작
- 음소거
- 게임 종료
- 점수 표시
- 최고 기록
- 다시 플레이
- 모바일 지원

키보드 조작이 의미 있는 게임은 키보드를 지원한다.

터치 조작이 의미 있는 게임은 모바일 터치를 지원한다.

게임 종료 후 다음 정보를 표시한다.

- 현재 점수
- 최고 기록
- 다시 플레이 버튼

같은 기능을 수행하는 시작 버튼이나 조작 버튼을 같은 화면에 불필요하게 중복 표시하지 않는다.

---

# 10. 입력 시스템

키보드 기반 게임은 가능한 한 공통 입력 시스템을 사용한다.

각 게임마다 독립적으로 전역 `keydown`, `keyup` 이벤트를 중복 등록하지 않는다.

게임 로직에서는 실제 키 코드보다 논리적 입력을 사용하는 것을 권장한다.

```text
left
right
up
down
action
pause
```

기본 키 매핑:

```text
left
- ArrowLeft
- A

right
- ArrowRight
- D

up
- ArrowUp
- W

down
- ArrowDown
- S

action
- Space
- Enter

pause
- Escape
```

게임 플레이 중 방향키와 Space로 인해 브라우저 페이지가 스크롤되지 않도록 처리한다.

단, 다음 요소에 사용자가 입력 중인 경우에는 기본 입력 동작을 방해하지 않는다.

- `input`
- `textarea`
- `select`
- `contenteditable`
- 기타 텍스트 입력 요소

---

# 11. 모바일 터치 입력

모바일에서 조작이 필요한 게임에는 충분히 큰 터치 영역을 제공한다.

터치 버튼은 너무 작거나 서로 지나치게 붙어 있지 않도록 한다.

가능하면 키보드 입력과 터치 입력이 같은 게임 액션으로 연결되도록 한다.

예:

```text
키보드 ArrowLeft
        ↓
      left
        ↑
모바일 왼쪽 버튼
```

게임 로직 내부에서 키보드와 터치 로직이 따로 분기되는 구조를 남발하지 않는다.

입력 장치만 다르고 게임 액션은 동일하게 처리하는 구조를 우선한다.

---

# 12. 게임 루프

실시간 게임의 메인 루프는 `requestAnimationFrame()`을 사용한다.

`setInterval()`을 화면 렌더링용 메인 게임 루프로 사용하지 않는다.

가능하면 다음처럼 게임 업데이트와 렌더링을 분리한다.

```text
update(deltaTime)
render()
```

이동 속도와 애니메이션은 특정 프레임 수가 아니라 경과 시간 기준으로 계산한다.

다음 환경에서 게임 속도가 크게 달라지지 않도록 한다.

- 60Hz
- 120Hz
- 144Hz
- 기타 고주사율 디스플레이

브라우저 탭이 비활성화되었다가 돌아오는 경우 지나치게 큰 `deltaTime`이 들어올 수 있으므로 필요한 경우 제한한다.

게임 종료 시 실행 중인 `requestAnimationFrame()`을 취소한다.

---

# 13. Canvas 규칙

Canvas를 사용하는 게임에서는 다음 원칙을 따른다.

- 화면 크기에 맞게 반응형으로 표시
- 필요하면 게임의 종횡비 유지
- CSS 표시 크기와 실제 Canvas 해상도를 구분
- `devicePixelRatio`를 고려해 흐릿한 화면 방지
- 게임 상태와 렌더링을 가능한 범위에서 분리
- 매 프레임 DOM 검색 금지
- 매 프레임 Canvas 크기 재설정 금지
- 반복 사용 리소스 캐싱
- 매 프레임 불필요한 객체 생성 최소화

Canvas에 모든 UI를 그리는 방식은 피한다.

메뉴, 버튼, 점수판, 설정 등은 HTML/CSS 사용을 우선한다.

---

# 14. 디자인 원칙

현재 사이트의 네온 사이버펑크 분위기를 유지한다.

주요 색상은 다음 계열을 유지한다.

- 검정
- 보라
- 형광 연두

제목에는 픽셀 또는 레트로 게임 느낌을 사용할 수 있다.

본문은 가독성을 우선한다.

게임 화면이 페이지에서 가장 눈에 띄도록 구성한다.

데스크톱의 큰 화면에서도 게임 영역이 지나치게 작아지지 않도록 한다.

모바일에서는 버튼과 터치 조작 영역을 충분히 크게 제공한다.

게임 플레이 영역은 특별한 요청이 없는 한 수평 레이아웃을 기본으로 한다.

버튼에는 짧고 경쾌한 상호작용 애니메이션을 적용할 수 있다.

과도한 장식으로 게임 플레이 가독성을 해치지 않는다.

불필요하게 복잡한 관리자 기능을 추가하지 않는다.

사용자의 요청 없이 전체 디자인 콘셉트를 변경하지 않는다.

---

# 15. 반응형 디자인

데스크톱과 모바일을 모두 고려한다.

모바일 지원을 추가하면서 기존 데스크톱 UI를 망가뜨리지 않는다.

반대로 데스크톱 화면을 위해 모바일 화면을 지나치게 축소하지 않는다.

게임 영역은 기기 크기에 맞춰 조절하되 내부 게임 좌표가 의도치 않게 찌그러지지 않도록 한다.

좁은 화면에서는 UI를 적절히 재배치한다.

모바일에서 다음 요소의 크기를 특히 확인한다.

- 시작 버튼
- 다시 시작 버튼
- 일시정지
- 음소거
- 방향 조작
- 액션 버튼

---

# 16. DOM 및 성능

사이트는 가볍게 유지한다.

다음을 피한다.

- 매 프레임 DOM 전체 재생성
- 매 프레임 반복적인 `querySelector`
- 과도한 레이아웃 재계산
- 동일한 이벤트 리스너 중복 등록
- 동일 이미지 반복 로딩
- 동일 사운드 반복 로딩
- 단순 기능을 위한 대형 라이브러리
- 불필요한 런타임 추상화

자주 사용하는 DOM 요소는 가능한 범위에서 캐싱한다.

화면 애니메이션에는 `requestAnimationFrame()`을 적절히 사용한다.

일반적인 UI 애니메이션은 가능한 경우 CSS를 사용한다.

가독성을 심하게 떨어뜨리는 미세 최적화는 하지 않는다.

---

# 17. CSS 규칙

CSS는 유지보수성과 반응형을 고려한다.

공통 UI는 공통 스타일을 사용한다.

게임 전용 스타일만 게임별로 분리한다.

JavaScript에서 불필요하게 많은 inline style을 생성하지 않는다.

다음 기능은 가능한 한 CSS가 담당한다.

- 레이아웃
- 색상
- 버튼 스타일
- 반응형
- hover
- active
- transition
- 모달
- HUD
- 일반 애니메이션

JavaScript는 CSS로 해결하기 어려운 동적 상태를 중심으로 처리한다.

---

# 18. localStorage

현재 기기의 `localStorage`를 이용해 각 게임의 최고 점수를 저장한다.

필요에 따라 다음 정보도 저장할 수 있다.

- 음량
- 음소거 상태
- 사용자 설정
- 게임 기록
- 간단한 진행 상태

가능하면 저장 기능은 공통 모듈로 관리한다.

저장된 데이터가 항상 정상이라고 가정하지 않는다.

다음 상황을 안전하게 처리한다.

- 값 없음
- 잘못된 JSON
- 예상하지 못한 타입
- 이전 버전 데이터
- 손상된 값

localStorage 오류 때문에 게임 전체가 실행되지 않는 일이 없도록 한다.

---

# 19. 사운드

사운드를 사용하는 게임은 다음 원칙을 따른다.

- 동일 오디오 인스턴스의 불필요한 중복 생성 방지
- 브라우저 자동 재생 제한 고려
- 사용자 입력 이후 사운드 시작
- 음소거 기능 제공
- 필요하면 음량 조절 기능 제공
- 게임 종료 또는 전환 시 불필요한 사운드 정리

음소거 상태는 필요하면 localStorage에 저장할 수 있다.

---

# 20. 리소스 관리

이미지, 사운드, 폰트 등 정적 리소스를 일관되게 관리한다.

같은 리소스를 여러 위치에 중복 저장하지 않는다.

가능하면 웹 친화적인 형식을 사용한다.

단, 사용자의 요청 없이 기존 이미지 품질이나 사이트의 시각적 느낌을 크게 변경하지 않는다.

이미지 최적화가 필요하면 품질 저하와 파일 크기의 균형을 고려한다.

---

# 21. Vite 규칙

개발 환경은 다음 명령으로 정상 실행되어야 한다.

```bash
npm install
npm run dev
```

배포용 빌드는 다음 명령으로 정상 완료되어야 한다.

```bash
npm run build
```

빌드 결과물은 정적 호스팅 환경에서 사용할 수 있어야 한다.

필요한 경우에만 추가적인 npm 패키지를 설치한다.

패키지를 추가할 때는 먼저 기존 브라우저 API나 현재 의존성으로 해결 가능한지 검토한다.

---

# 22. GitHub Pages 배포 주의사항

GitHub Pages를 기본 공개 배포 방식으로 사용한다.

기본 주소:

```text
https://gla9940.github.io/dolldom-arcade/
```

사이트가 도메인 루트 `/`가 아니라 다음 하위 경로에 배포된다는 점을 항상 고려한다.

```text
/dolldom-arcade/
```

따라서 다음과 같은 도메인 루트 절대 경로 사용을 피한다.

```text
/app.js
/og.png
/assets/image.png
```

가능하면 Vite가 올바른 경로를 생성하도록 한다.

필요한 경우 `vite.config.js`의 `base` 설정을 확인한다.

예:

```js
export default {
  base: "/dolldom-arcade/",
};
```

또는 현재 배포 구조에 더 적합한 방식을 사용한다.

소스 코드 내부에서는 Vite의 import 및 asset 처리 방식을 우선한다.

기존 코드에서 상대 경로가 필요한 경우 다음과 같은 형태를 사용할 수 있다.

```text
./app.js
./og.png
```

단, Vite 빌드 구조로 전환한 이후에는 임의로 경로를 하드코딩하기 전에 실제 빌드 결과를 확인한다.

---

# 23. GitHub Pages 배포 전 검증

`main` 브랜치에 푸시하기 전에 가능한 범위에서 다음을 확인한다.

```bash
npm run build
```

그리고 주요 게임 흐름을 확인한다.

- 사이트 진입
- 게임 선택
- 게임 시작
- 키보드 입력
- 터치 입력
- 일시정지
- 다시 시작
- 음소거
- 게임 종료
- 최고 점수 저장
- 다시 플레이
- 게임 간 이동
- 모바일 레이아웃
- 데스크톱 레이아웃

검증된 변경사항을 `main` 브랜치에 푸시하면 기존 GitHub Pages 배포 설정을 통해 배포되도록 유지한다.

사용자의 요청 없이 기존 GitHub Pages 배포 방식을 대규모로 변경하지 않는다.

---

# 24. Codex Sites 백업 배포

Codex Sites는 GitHub Pages의 백업 배포 방식으로 사용한다.

기존 `.openai/hosting.json`을 삭제하지 않는다.

기존 `.openai/hosting.json`을 이유 없이 새로 만들거나 덮어쓰지 않는다.

파일에 저장된 기존 Sites 프로젝트 ID를 유지한다.

동일한 로컬 사이트에 새로운 Codex Sites 프로젝트를 중복 생성하지 않는다.

Codex Sites 배포는 사용자가 명시적으로 요청했을 때만 수행한다.

예:

```text
Codex Sites에도 배포해줘
백업 사이트도 업데이트해줘
```

배포 전 다음을 확인한다.

1. 현재 소스 상태
2. 로컬 빌드
3. 기존 Sites 프로젝트 연결 상태

새 프로젝트를 만드는 대신 기존 Sites 프로젝트의 새로운 버전으로 저장하고 배포한다.

사용자의 요청 없이 다음 항목을 변경하지 않는다.

- 사이트 접근 권한
- 공개/비공개 상태
- 사용자 도메인
- Sites 프로젝트 ID

사이트 제목 변경과 공개 URL 변경을 같은 작업으로 취급하지 않는다.

---

# 25. 기존 기능 유지

기존에 정상적으로 동작하는 기능을 유지하는 것을 최우선으로 한다.

기존 코드를 수정하기 전에 먼저 확인한다.

1. 현재 파일 구조
2. 현재 구현 방식
3. 최근 변경사항
4. 기존 기능의 목적
5. 현재 디자인 의도

정상적으로 작동하는 코드는 가능한 한 재사용한다.

더 깔끔해 보인다는 이유만으로 전체 구현을 다시 작성하지 않는다.

대규모 리팩터링보다 점진적인 리팩터링을 우선한다.

사용자의 요청 없이 다음 요소를 제거하지 않는다.

- 기존 기능
- 게임
- 이미지
- 사운드
- UI
- 게임 조작
- 디자인 요소

---

# 26. 작업 범위

요청 범위를 벗어난 파일이나 기능을 임의로 수정하지 않는다.

하나의 버그 수정이나 기능 추가 요청을 받은 경우 수정 범위를 가능한 한 작게 유지한다.

다음을 피한다.

- 관련 없는 디자인 변경
- 관련 없는 파일명 변경
- 전체 코드 포맷 변경
- 불필요한 폴더 이동
- 필요 없는 구조 개편
- 요청하지 않은 기능 추가

사용자가 직접 변경한 내용이 있는 경우 해당 변경사항을 우선 보존한다.

---

# 27. 코드 스타일

코드는 단순하고 명확하게 작성한다.

복잡하고 영리한 코드보다 읽기 쉬운 코드를 우선한다.

변수명과 함수명은 역할을 알기 쉽게 작성한다.

기본적으로 `const`를 사용한다.

재할당이 필요한 경우에만 `let`을 사용한다.

`var`는 사용하지 않는다.

가능한 경우 함수의 역할을 작게 유지한다.

가독성이 좋아지는 경우 early return을 사용할 수 있다.

더 이상 사용하지 않는 코드를 장기간 주석 처리 상태로 남기지 않는다.

주석은 코드 자체에서 알기 어려운 이유와 의도를 설명할 때 사용한다.

---

# 28. 오류 처리

예상 가능한 오류를 아무 처리 없이 무시하지 않는다.

다음 상황을 적절하게 처리한다.

- 이미지 로딩 실패
- 사운드 로딩 실패
- 잘못된 localStorage 값
- 초기화 실패
- 브라우저 API 미지원
- 게임 리소스 누락

개발 과정에서 필요한 로그는 사용할 수 있다.

작업 완료 전 임시 디버깅용 `console.log`는 가능한 범위에서 제거한다.

정상적인 게임 플레이 중 콘솔을 불필요하게 계속 출력하지 않는다.

---

# 29. 작업 후 검증

의미 있는 코드 변경 후 가능한 범위에서 다음을 실행한다.

```bash
npm run build
```

필요하면 개발 서버에서도 확인한다.

```bash
npm run dev
```

다음 문제를 확인한다.

- JavaScript 오류
- import 오류
- asset 경로 오류
- GitHub Pages 경로 오류
- 중복 이벤트 리스너
- 게임 루프 중복 실행
- 게임 전환 후 이전 게임 실행 여부
- 키보드 입력
- 모바일 터치
- 반응형 레이아웃
- localStorage
- 음소거
- 게임 재시작
- 기존 기능 회귀

실제로 실행하지 않은 빌드나 테스트를 성공했다고 보고하지 않는다.

---

# 30. 작업 완료 보고

코드 작업을 완료한 뒤 다음 내용을 간단하게 알려준다.

- 무엇을 변경했는지
- 중요하게 수정한 파일
- 새로 만든 파일
- 삭제한 파일이 있다면 그 이유
- `npm run build` 실행 여부
- 빌드 성공 여부
- 직접 확인한 주요 기능
- 남아 있는 문제 또는 주의사항

작은 변경마다 별도의 보고서 파일을 만들지 않는다.

작업 결과는 대화에서 간단하게 설명한다.

---

# 31. AGENTS.md 수정 규칙

이 문서는 프로젝트 전체에 반복적으로 적용되는 장기적인 규칙을 기록한다.

일회성 작업 지시를 이 파일에 추가하지 않는다.

예를 들어 다음 내용은 AGENTS.md에 넣지 않는다.

```text
오늘 러닝 게임의 속도를 10% 낮춰라.
이번 작업에서 버튼 위치를 바꿔라.
이번에만 이미지를 새 이미지로 교체해라.
```

반대로 다음처럼 앞으로도 계속 적용할 규칙은 추가할 수 있다.

```text
모든 게임은 모바일 터치를 지원한다.
게임 종료 시 이벤트 리스너를 정리한다.
새 게임은 src/games 아래에서 관리한다.
GitHub Pages 하위 경로를 항상 고려한다.
```

AGENTS.md를 수정할 때 기존 프로젝트 규칙을 불필요하게 삭제하지 않는다.

---

# 32. 새 미니게임 추가 규칙

새로운 게임을 추가할 때 다음 순서를 권장한다.

1. 현재 공통 시스템 확인
2. 기존 게임에서 재사용 가능한 기능 확인
3. `src/games/` 아래에 게임 모듈 생성
4. 게임 전용 상태 구현
5. 입력 연결
6. 게임 루프 구현
7. 렌더링 구현
8. 점수 및 localStorage 연결
9. 일시정지 및 다시 시작 구현
10. 종료 및 cleanup 구현
11. 모바일 대응
12. 빌드 확인

게임 특성에 따라 Canvas 또는 HTML/CSS를 선택한다.

Canvas를 사용한다는 이유만으로 사이트 전체 구조를 Canvas 중심으로 바꾸지 않는다.

---

# 33. 최종 우선순위

작업 중 판단이 필요한 경우 다음 우선순위를 따른다.

1. 기존 기능과 사용자 변경사항 보존
2. 게임의 정확한 동작
3. 기존 디자인 유지
4. 단순하고 유지보수하기 좋은 구조
5. 좋은 게임 플레이 경험
6. 데스크톱과 모바일 반응형
7. 성능
8. GitHub Pages 호환성
9. 작은 번들 크기와 적은 의존성
10. 시각적 완성도

성능을 조금 높이기 위해 코드 구조를 과도하게 복잡하게 만들지 않는다.

불필요한 최적화를 위해 정확성이나 유지보수성을 희생하지 않는다.
